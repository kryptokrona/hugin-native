import * as Sizes from './sizes';

import { ViewStyle } from 'react-native';

export const baseContainer: ViewStyle = {};

export const baseScreen: ViewStyle = {
  flex: 1,
  flexDirection: 'column',
};
