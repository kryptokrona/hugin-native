import { Text } from 'react-native';

import { ScreenLayout } from '@/components';

interface Props {}

export const RecipientsScreen: React.FC<Props> = () => {
  return (
    <ScreenLayout>
      <Text>Recipients Screen</Text>
    </ScreenLayout>
  );
};
