import { Text } from 'react-native';

import { ScreenLayout } from '@/components';

interface Props {}

export const TransferScreen: React.FC<Props> = () => {
  return (
    <ScreenLayout>
      <Text>Transfer Screen</Text>
    </ScreenLayout>
  );
};
